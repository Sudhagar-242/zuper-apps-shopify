<style>
  /* Top Section */
  .top-section {
    position: relative;
    height: 587px;
    color: white;
    padding: 40px 60px;
    background-position: center;
    display: flex;
    flex-direction: row;
    background: linear-gradient(
        360deg,
        rgba(0, 0, 0, 0) 64.4%,
        rgba(0, 0, 0, 0.8) 100%
      ),
      linear-gradient(0deg, rgba(0, 0, 0, 0.12), rgba(0, 0, 0, 0.12));
    background-repeat: no-repeat;
    width: 100%;
    background-size: cover;
  }

  .top-section h1 {
    font-size: 42px;
    margin: 0 0 20px;
    font-weight: 700;
    line-height: 140%;
    padding-top: 24px;
    color: #fff;
    position: relative;
    z-index: 1;
  }

  .top-section .description-box {
    max-width: 450px;
    background: #1c191acc;
    padding: 34px 42px 27px 27px;
    border-radius: 7px;
    line-height: 23px;
    font-size: 16px;
    font-weight: 400;
    margin-left: auto;
    max-height: max-content;
    margin-top: 85px;
    letter-spacing: -0.03px;
    margin-right: 80px;
    line-height: 28px;
    position: relative;
    z-index: 1;
  }
  .description-box p {
    margin: 0;
    padding-bottom: 29px;
  }
  .top-section .btn-explore {
    background-color: #ed1c24;
    border: none;
    padding: 10px 20px;
    font-weight: 700;
    font-size: 16px;
    color: white;
    border-radius: 4px;
    cursor: pointer;
    transition: 0.5s;
  }
  .top-section .btn-explore:hover {
    background-color: #fff;
    color: #000;
    transition: 0.5s;
  }

  /* Bottom tabss */
  .tabss-wrapper {
    display: flex;
    gap: 33px;
  }

  .tabs {
    background: white;
    border-radius: 12px;
    box-shadow: 0 2px 12px rgb(0 0 0 / 0.1);
    width: 373px;
    text-align: center;
    padding: 36px 33px;
    cursor: pointer;
    transition: background-color 0.3s ease;
    position: relative;
    display: flex !important;
    align-items: center;
    gap: 49px;
    transition: 0.5s;
    border: 0.7px solid transparent;
  }
  .tabs:hover {
    background-color: #b22c28;
    color: white;
    font-weight: 700;
    box-shadow: 0px 15px 38px 0px #b1b1b140;
    border: 0.7px solid #dbdbdb;
    transition: 0.5s;
  }
  .tabs:hover h3 {
    color: #fff;
    transition: 0.5s;
  }
  .tabs.active {
    background-color: #b22c28;
    color: white;
    font-weight: 700;
    box-shadow: 0px 15px 38px 0px #b1b1b140;
    border: 0.7px solid #dbdbdb;
  }
  .tabs.active h3 {
    color: white;
    transition: 0.5s;
  }
  .tabs h3 {
    font-weight: 700;
    font-size: 20px;
    user-select: none;
    color: #1c191a;
    margin: 0;
  }
  .tabs img {
    width: 110px;
    height: 110px;
    border-radius: 50%;
    object-fit: cover;
    border: 3px solid transparent;
    transition: border-color 0.3s ease;
  }
  .top-section:after {
    content: "";
    background: linear-gradient(
        360deg,
        rgba(0, 0, 0, 0) 64.4%,
        rgba(0, 0, 0, 0.8) 100%
      ),
      linear-gradient(0deg, rgba(0, 0, 0, 0.12), rgba(0, 0, 0, 0.12));
    width: 100%;
    position: absolute;
    left: 0;
    right: 0;
    height: 100%;
    top: 0;
  }
  .slider-nav {
    display: none;
  }
  /* Mobile .slider */
  @media screen and (max-width: 989px) {
    .tabss-wrapper {
      flex-wrap: nowrap;
      overflow-x: auto;
      scroll-snap-type: x mandatory;
      gap: 15px;
      padding: 20px 15px;
    }
    .tabss-wrapper::-webkit-scrollbar {
      display: none;
    }
    .tabs {
      flex: 0 0 100%;
      scroll-snap-align: center;
    }
    .slider-nav {
      display: flex;
      justify-content: center;
      gap: 10px;
      margin-top: 10px;
    }
    .slider-nav button {
      background: transparent;
      border-radius: 30px;
      cursor: pointer;
      border: 0.8px solid #000000;
      width: 46px;
      height: 46px;
      display: flex;
      align-items: center;
      justify-content: center;
    }
  }

  @media screen and (max-width: 989px) {
    .top-section {
      height: auto;
      padding: 0px 20px;
      flex-direction: column;
    }
    .top-section h1 {
      font-size: 38px;
    }
    .tabss-wrapper {
      justify-content: flex-start;
    }
    .top-section .description-box {
      margin-right: 0;
      margin-top: 0;
      padding: 20px;
      margin-bottom: 30px;
    }
    .tabss-wrapper {
      margin-top: auto;
    }
  }
  /* Desktop slider */
  @media screen and (min-width: 990px) {
    .tabss-wrapper {
      overflow-x: auto;
      scroll-snap-type: x mandatory;

      scroll-behavior: smooth;
      max-width: 1188px;
      margin: -90px auto;
      flex-wrap: nowrap;
      justify-content: normal;
      scrollbar-width: none;
      padding-bottom: 20px;
    }

    .tabs {
      flex: 0 0 373px; /* width of each card */
      scroll-snap-align: start;
    }

    .slider-nav {
      display: flex;
      justify-content: center;
      gap: 10px;
      position: relative;
      max-width: 1300px;
      margin: 10px auto;
    }
    #slidePrev,
    #slideNext {
      position: absolute;
      top: -24px;
      transition: 0.5s;
    }
    #slidePrev {
      left: 0;
    }
    #slideNext {
      right: 0;
    }
    .slider-nav button {
      background: transparent;
      border-radius: 30px;
      cursor: pointer;
      border: 0.8px solid #000000;
      width: 46px;
      height: 46px;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    #slidePrev:hover,
    #slideNext:hover {
      transition: 0.5s;
      transform: scale(1.1);
    }
  }
</style>

<div class="container">
  <div
    id="topSection"
    class="top-section"
    style="
                      background-image: url('https://cdn.shopify.com/s/files/1/0927/8583/6398/files/agriculture-healthy-food.png?v=1752959180');
    "
  >
    <h1 id="topTitle">Agriculture</h1>
    <div class="description-box">
      <p id="topDescription">
        Our advanced agricultural solutions are designed to optimize performance in the field — whether it's through
        smart engineering, rugged design, or cutting-edge machinery integration. From seeding to storage, we deliver
        reliability where it matters most.
      </p>
      <button class="btn-explore">Explore All Products</button>
    </div>
  </div>

  <!-- Slider Tabs -->
  <div class="tabss-wrapper">
    {% for block in section.blocks %}
      <div class="tabs active" data-category="agriculture">
        <img src="{{ block.settings.rounded_image | image_url }}" alt="{{ block.settings.heading_text }}">
        <h3>{{ block.settings.heading_text }}</h3>
        <p hidden>
          <span id="hidden_block_title"> {{ block.settings.heading_text }} </span>
          <span id="hidden_block_description"> {{ block.settings.description }} </span>
          <a id="hidden_block_button" href="{{ block.settings.button_link }}">
            {{ block.settings.button_label }}
          </a>
          <img
            id="hidden_block_background_image"
            src="{{ block.settings.rounded_image | image_url }}" alt="{{ block.settings.heading_text }}"
          >
        </p>
      </div>
    {% endfor %}
  </div>

  <!-- Mobile/Desktop Arrow Buttons -->
  <div class="slider-nav">
    <button id="slidePrev">
      <img
        src="https://cdn.shopify.com/s/files/1/0927/8583/6398/files/Arrow_1.svg?v=1753248645"
      >
    </button>
    <button id="slideNext">
      <img
        src="https://cdn.shopify.com/s/files/1/0927/8583/6398/files/Arrow_14.svg?v=1754980108"
      >
    </button>
  </div>
</div>

{% javascript %}
  const tabss = document.querySelectorAll('.tabs');
  const wrapper = document.querySelector('.tabss-wrapper');
  const topSection = document.getElementById('topSection');
  const topTitle = document.getElementById('topTitle');
  const topDescription = document.getElementById('topDescription');
  const sliderNav = document.querySelector('.slider-nav');

  // Function to show/hide arrows based on desktop & tab count
  function updateArrows() {
    if (window.innerWidth >= 990) {
      // Desktop
      if (tabss.length <= 3) {
        sliderNav.style.display = 'none'; // Hide arrows
      } else {
        sliderNav.style.display = 'flex'; // Show arrows
      }
    } else {
      // Mobile: always show arrows
      sliderNav.style.display = 'flex';
    }
  }

  // Run on load
  updateArrows();

  // Update on resize
  window.addEventListener('resize', updateArrows);

  // Function to update active tab + top section
  function setActiveTab(tab) {
    tabss.forEach((t) => t.classList.remove('active'));
    tab.classList.add('active');
    const hiddenData = {
      title: tab.querySelector('#hidden_block_title')?.textContent.trim(),
      description: tab.querySelector('#hidden_block_description')?.textContent.trim(),
      button: {
        text: tab.querySelector('#hidden_block_button')?.textContent.trim(),
        url: tab.querySelector('#hidden_block_button')?.getAttribute('href'),
      },
      backgroundImage: tab.querySelector('#hidden_block_background_image')?.getAttribute('src'),
    };

    console.log(hiddenData);

    console.log(tab);
    // const category = tab.getAttribute("data-category");
    // const data = categories[category];

    // topSection.style.backgroundImage = `url('${data.image}')`;
    // topTitle.textContent = data.title;
    // topDescription.textContent = data.description;
  }

  // Click on tabs manually
  tabss.forEach((tab) => {
    tab.addEventListener('click', () => setActiveTab(tab));
  });

  // Helper to get the tab currently in view (for slider)
  function getVisibleTab() {
    let closestTab = null;
    let minDistance = Infinity;
    const wrapperCenter = wrapper.scrollLeft + wrapper.offsetWidth / 2;

    tabss.forEach((tab) => {
      const tabCenter = tab.offsetLeft + tab.offsetWidth / 2;
      const distance = Math.abs(wrapperCenter - tabCenter);
      if (distance < minDistance) {
        minDistance = distance;
        closestTab = tab;
      }
    });
    console.log(closestTab);
    return closestTab;
  }

  // Arrow scroll (works for both desktop & mobile)
  const slideAmount = 320; // adjust according to card width + gap
  document.getElementById('slidePrev').addEventListener('click', () => {
    wrapper.scrollBy({ left: -slideAmount, behavior: 'smooth' });
    setTimeout(() => {
      const tab = getVisibleTab();
      if (tab) setActiveTab(tab);
    }, 300);
  });

  document.getElementById('slideNext').addEventListener('click', () => {
    wrapper.scrollBy({ left: slideAmount, behavior: 'smooth' });
    setTimeout(() => {
      const tab = getVisibleTab();
      if (tab) setActiveTab(tab);
    }, 300);
  });
{% endjavascript %}

{% schema %}
{
  "name": "Custom Image Tab",
  "settings": [],
  "blocks": [
    {
      "name": "Image Tab",
      "type": "image_tab",
      "settings": [
        {
          "type": "header",
          "content": "Image"
        },
        {
          "type": "image_picker",
          "id": "background_image",
          "label": "Background Image",
          "info": "This will be the image exist in top section"
        },
        {
          "type": "image_picker",
          "id": "rounded_image",
          "label": "Rounded Tab Image",
          "info": "This will be the image exist in taps"
        },
        {
          "type": "header",
          "content": "Title In Top Section"
        },
        {
          "type": "text",
          "id": "heading_text",
          "label": "Header Text",
          "default": "Header",
          "info": "This will be exist in top section"
        },
        {
          "type": "header",
          "content": "Description In Top Section"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Description",
          "default": "Welcome to My Shop"
        },
        {
          "type": "header",
          "content": "Button"
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Button Label",
          "default": "Explore All Products"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Custom Image Tab",
      "blocks": [{ "type": "image_tab" }, { "type": "image_tab" }, { "type": "image_tab" }]
    }
  ]
}
{% endschema %}
