<style>
  /* Top Section */
  .top-section {
    position: relative;
    height: 587px;
    color: white;
    padding: 40px 60px;
    background-position: center;
    display: flex;
    flex-direction: row;
    background: linear-gradient(
        360deg,
        rgba(0, 0, 0, 0) 64.4%,
        rgba(0, 0, 0, 0.8) 100%
      ),
      linear-gradient(0deg, rgba(0, 0, 0, 0.12), rgba(0, 0, 0, 0.12));
    background-repeat: no-repeat;
    width: 100%;
    background-size: cover;
  }

  .top-section h1 {
    font-size: 42px;
    margin: 0 0 20px;
    font-weight: 700;
    line-height: 140%;
    padding-top: 24px;
    color: #fff;
    position: relative;
    z-index: 1;
  }

  .top-section .description-box {
    max-width: 450px;
    background: #1c191acc;
    padding: 34px 42px 27px 27px;
    border-radius: 7px;
    line-height: 23px;
    font-size: 16px;
    font-weight: 400;
    margin-left: auto;
    max-height: max-content;
    margin-top: 85px;
    letter-spacing: -0.03px;
    margin-right: 80px;
    line-height: 28px;
    position: relative;
    z-index: 1;
  }
  .description-box p {
    margin: 0;
    padding-bottom: 29px;
  }
  .top-section .btn-explore {
    background-color: #ed1c24;
    border: none;
    padding: 10px 20px;
    font-weight: 700;
    font-size: 16px;
    color: white;
    border-radius: 4px;
    cursor: pointer;
    transition: 0.5s;
    text-decoration: none;
  }
  .top-section .btn-explore:hover {
    background-color: #fff;
    color: #000;
    transition: 0.5s;
    text-decoration: none;
  }

  /* Bottom tabss */
  .tabs-wrapper {
    display: flex;
    gap: 33px;
  }

  .tabs {
    background: white;
    border-radius: 12px;
    box-shadow: 0 2px 12px rgb(0 0 0 / 0.1);
    width: 373px;
    text-align: center;
    padding: 36px 33px;
    cursor: pointer;
    transition: background-color 0.3s ease;
    position: relative;
    display: flex !important;
    align-items: center;
    gap: 49px;
    transition: 0.5s;
    border: 0.7px solid transparent;
  }
  .tabs:hover {
    background-color: #b22c28;
    color: white;
    font-weight: 700;
    box-shadow: 0px 15px 38px 0px #b1b1b140;
    border: 0.7px solid #dbdbdb;
    transition: 0.5s;
  }
  .tabs:hover h3 {
    color: #fff;
    transition: 0.5s;
  }
  .tabs.active {
    background-color: #b22c28;
    color: white;
    font-weight: 700;
    box-shadow: 0px 15px 38px 0px #b1b1b140;
    border: 0.7px solid #dbdbdb;
  }
  .tabs.active h3 {
    color: white;
    transition: 0.5s;
  }
  .tabs h3 {
    font-weight: 700;
    font-size: 20px;
    user-select: none;
    color: #1c191a;
    margin: 0;
  }
  .tabs img {
    width: 110px;
    height: 110px;
    border-radius: 50%;
    object-fit: cover;
    border: 3px solid transparent;
    transition: border-color 0.3s ease;
  }
  .top-section:after {
    content: "";
    background: linear-gradient(
        360deg,
        rgba(0, 0, 0, 0) 64.4%,
        rgba(0, 0, 0, 0.8) 100%
      ),
      linear-gradient(0deg, rgba(0, 0, 0, 0.12), rgba(0, 0, 0, 0.12));
    width: 100%;
    position: absolute;
    left: 0;
    right: 0;
    height: 100%;
    top: 0;
  }
  .slider-nav {
    display: none;
  }
  /* Mobile .slider */
  @media screen and (max-width: 989px) {
    .tabs-wrapper {
      flex-wrap: nowrap;
      overflow-x: auto;
      scroll-snap-type: x mandatory;
      gap: 15px;
      padding: 20px 15px;
    }
    .tabs-wrapper::-webkit-scrollbar {
      display: none;
    }
    .tabs {
      flex: 0 0 100%;
      scroll-snap-align: center;
    }
    .slider-nav {
      display: flex;
      justify-content: center;
      gap: 10px;
      margin-top: 10px;
    }
    .slider-nav button {
      background: transparent;
      border-radius: 30px;
      cursor: pointer;
      border: 0.8px solid #000000;
      width: 46px;
      height: 46px;
      display: flex;
      align-items: center;
      justify-content: center;
    }
  }

  @media screen and (max-width: 989px) {
    .top-section {
      height: auto;
      padding: 0px 20px;
      flex-direction: column;
    }
    .top-section h1 {
      font-size: 38px;
    }
    .tabs-wrapper {
      justify-content: flex-start;
    }
    .top-section .description-box {
      margin-right: 0;
      margin-top: 0;
      padding: 20px;
      margin-bottom: 30px;
    }
    .tabs-wrapper {
      margin-top: auto;
    }
  }
  /* Desktop slider */
  @media screen and (min-width: 990px) {
    .tabs-wrapper {
      overflow-x: auto;
      scroll-snap-type: x mandatory;

      scroll-behavior: smooth;
      max-width: 1188px;
      margin: -90px auto;
      flex-wrap: nowrap;
      justify-content: normal;
      scrollbar-width: none;
      padding-bottom: 20px;
    }

    .tabs {
      flex: 0 0 373px; /* width of each card */
      scroll-snap-align: start;
    }

    .slider-nav {
      display: flex;
      justify-content: center;
      gap: 10px;
      position: relative;
      max-width: 1300px;
      margin: 10px auto;
    }
    #slidePrev,
    #slideNext {
      position: absolute;
      top: -24px;
      transition: 0.5s;
    }
    #slidePrev {
      left: 0;
    }
    #slideNext {
      right: 0;
    }
    .slider-nav button {
      background: transparent;
      border-radius: 30px;
      cursor: pointer;
      border: 0.8px solid #000000;
      width: 46px;
      height: 46px;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    #slidePrev:hover,
    #slideNext:hover {
      transition: 0.5s;
      transform: scale(1.1);
    }
  }
</style>

<div class="container">
  <div
    id="topSection"
    class="top-section"
    style="
                      background-image: url('https://cdn.shopify.com/s/files/1/0927/8583/6398/files/agriculture-healthy-food.png?v=1752959180');
    "
  >
    <h1 id="topTitle">Agriculture</h1>
    <div class="description-box">
      <p id="topDescription">
        Our advanced agricultural solutions are designed to optimize performance in the field — whether it's through
        smart engineering, rugged design, or cutting-edge machinery integration. From seeding to storage, we deliver
        reliability where it matters most.
      </p>
      <a href="" class="btn-explore">Explore All Products</a>
    </div>
  </div>

  <!-- Slider Tabs -->
  <div class="tabs-wrapper">
    {% for block in section.blocks %}
        <div
  class="tabs"
  data-category="agriculture"
  data-title="{{ block.settings.heading_text | escape }}"
  data-description="{{ block.settings.description | strip_newlines | escape }}"
  data-button-text="{{ block.settings.button_label | escape }}"
  data-button-url="{{ block.settings.button_link }}"
  data-background-image="{{ block.settings.background_image | image_url }}"
>
  <img
    src="{{ block.settings.rounded_image | image_url }}"
    alt="{{ block.settings.heading_text | escape }}"
  />
  <h3>{{ block.settings.heading_text }}</h3>
</div>
    {% endfor %}
  </div>

  <!-- Mobile/Desktop Arrow Buttons -->
  <div class="slider-nav">
    <button id="slidePrev">
      <img
        src="https://cdn.shopify.com/s/files/1/0927/8583/6398/files/Arrow_1.svg?v=1753248645"
      >
    </button>
    <button id="slideNext">
      <img
        src="https://cdn.shopify.com/s/files/1/0927/8583/6398/files/Arrow_14.svg?v=1754980108"
      >
    </button>
  </div>
</div>

{% javascript %}
  // Selectors for tabs and UI elements
  const tabs = document.querySelectorAll('.tabs');
  const wrapper = document.querySelector('.tabs-wrapper');
  const topSection = document.getElementById('topSection');
  const topTitle = document.getElementById('topTitle');
  const topDescription = document.getElementById('topDescription');
  const topButton = document.querySelector('.btn-explore');
  const sliderNav = document.querySelector('.slider-nav');
  const slideAmount = 320; // Horizontal scroll step for arrows

  // Show or hide navigation arrows based on screen width and tab count
  function toggleArrowsVisibility() {
    const isDesktop = window.innerWidth >= 990;
    if (isDesktop) {
      sliderNav.style.display = (tabs.length > 3) ? 'flex' : 'none';
    } else {
      sliderNav.style.display = 'flex';
    }
  }

  // Activate a tab and update the top section with tab-specific content
  function activateTab(tab) {
    tabs.forEach(t => t.classList.remove('active'));
    tab.classList.add('active');

    const content = {
      title: tab.getAttribute('data-title'),
      description: tab.getAttribute('data-description'),
      buttonText: tab.getAttribute('data-button-text'),
      buttonUrl: tab.getAttribute('data-button-url'),
      backgroundImage: tab.getAttribute('data-background-image')
    };

    topSection.style.backgroundImage = `url('${content.backgroundImage}')`;
    topTitle.textContent = content.title;
    topDescription.textContent = content.description;
    topButton.textContent = content.buttonText;
    topButton.href = content.buttonUrl;
  }

  // Determine which tab is closest to the center of the wrapper viewport
function getCenteredTab() {
  let closest = null;
  let smallestDistance = Infinity;
  const wrapperScrollLeft = wrapper.scrollLeft;
  const wrapperWidth = wrapper.offsetWidth;
  const halfWrapperWidth = wrapperWidth / 2;
  const wrapperCenter = wrapperScrollLeft + halfWrapperWidth;

  console.group('Calculating centered tab');
  console.log(`Wrapper scrollLeft: ${wrapperScrollLeft}`);
  console.log(`Wrapper width: ${wrapperWidth}`);
  console.log(`Half wrapper width: ${halfWrapperWidth}`);
  console.log(`Calculated wrapper center position (scrollLeft + halfWidth): ${wrapperCenter}`);
  
  tabs.forEach((tab, index) => {
    const tabOffsetLeft = tab.offsetLeft;
    const tabWidth = tab.offsetWidth;
    const halfTabWidth = tabWidth / 2;
    const tabCenter = tabOffsetLeft + halfTabWidth;
    const distance = Math.abs(wrapperCenter - tabCenter);

    console.group(`Tab ${index + 1} details`);
    console.log(`Tab offsetLeft: ${tabOffsetLeft}`);
    console.log(`Tab width: ${tabWidth}`);
    console.log(`Half of tab width: ${halfTabWidth}`);
    console.log(`Calculated tab center position (offsetLeft + halfWidth): ${tabCenter}`);
    console.log(`Distance from wrapper center: |${wrapperCenter} - ${tabCenter}| = ${distance}`);

    if (distance < smallestDistance) {
      smallestDistance = distance;
      closest = tab;
      console.log(`🟢 New closest tab selected (smallestDistance updated to ${smallestDistance})`);
    } else {
      console.log(closest.classList);
      if (closest.classList) {
        console.log(index);
      }
      console.log(`🔴 Distance ${distance} not less than current smallestDistance ${smallestDistance}`);
    }
    console.groupEnd();
  });

  if (closest) {
    console.log('Final closest tab element:', closest);
  } else {
    console.warn('No tabs found to select!');
  }
  console.groupEnd();

  return closest;
}


  // Scroll wrapper left or right and update active tab accordingly
  function scrollWrapper(amount) {
    wrapper.scrollBy({ left: amount, behavior: 'smooth' });
    setTimeout(() => {
      const centeredTab = getCenteredTab();
      if (centeredTab) activateTab(centeredTab);
    }, 300);
  }

  // Initialize on page load
  function init() {
    toggleArrowsVisibility();

    // Set initial active tab if none active
    if (!document.querySelector('.tabs.active') && tabs.length > 0) {
      activateTab(tabs[0]);
    }

    // Event listeners for tab clicks
    tabs.forEach(tab => tab.addEventListener('click', () => activateTab(tab)));

    // Arrow button click handlers
    document.getElementById('slidePrev').addEventListener('click', () => scrollWrapper(-slideAmount));
    document.getElementById('slideNext').addEventListener('click', () => scrollWrapper(slideAmount));

    // Update arrows visibility on window resize
    window.addEventListener('resize', toggleArrowsVisibility);
  }

  // Run initialization
  init();
{% endjavascript %}


{% schema %}
{
  "name": "Custom Image Tab",
  "settings": [],
  "blocks": [
    {
      "name": "Image Tab",
      "type": "image_tab",
      "settings": [
        {
          "type": "header",
          "content": "Image"
        },
        {
          "type": "image_picker",
          "id": "background_image",
          "label": "Background Image",
          "info": "This will be the image exist in top section"
        },
        {
          "type": "image_picker",
          "id": "rounded_image",
          "label": "Rounded Tab Image",
          "info": "This will be the image exist in taps"
        },
        {
          "type": "header",
          "content": "Title In Top Section"
        },
        {
          "type": "text",
          "id": "heading_text",
          "label": "Header Text",
          "default": "Header",
          "info": "This will be exist in top section"
        },
        {
          "type": "header",
          "content": "Description In Top Section"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Description",
          "default": "Welcome to My Shop"
        },
        {
          "type": "header",
          "content": "Button"
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Button Label",
          "default": "Explore All Products"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Custom Image Tab",
      "blocks": [{ "type": "image_tab" }, { "type": "image_tab" }, { "type": "image_tab" }]
    }
  ]
}
{% endschema %}
